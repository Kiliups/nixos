{ ... }:
{
  # Replace with hardware generated by:
  # nixos-generate-config --show-hardware-config > private/hosts/<host>/hardware-configuration.nix
  fileSystems."/" = {
    device = "/dev/disk/by-label/nixos";
    fsType = "ext4";
  };
}
